#include<stdio.h>
#include<string.h>
#include<stdbool.h>
#include<time.h>
#include <stdlib.h>
int main()
{
    srand(time(NULL));
    int contatore=1;
    int scelta=0;
    int min = 0;
    int max = 500;
    int numero_scelto = (rand() % (max - min + 1)) + min;
    printf("indovina il numero intero da 0 a 500\n");
    printf("inserisci un numero intero");
    scanf("%d", &scelta);
    while(scelta!=numero_scelto){
    if (scelta>numero_scelto)
    {
    printf("troppo grande");
    }
    else {
    printf("troppo piccolo");
    
    }
    scanf("%d",&scelta);
    contatore++;
    
    
    }
    printf("ce lhai fatta, bravo\n");
    printf("in solo %d tentativi",contatore);
    return 0;
}
